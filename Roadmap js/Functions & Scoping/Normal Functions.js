//functions

function sayHi() {
  //define functions
  console.log("Hi there");
}

sayHi(); // ecexute function

function sayHi2(a, b) {
  //define with params
  console.log(a + b);
}
sum(1,2) // calling the function with the parameters

function sum(a,b){
    return a+b //whenever u use the function keyword return its conna store the information in the varianle assigned. and everything after return is gonna stop
}

const value = sum(4,5) // here is a example