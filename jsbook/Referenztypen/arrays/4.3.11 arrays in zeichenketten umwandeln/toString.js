// arrays in strings verwandeln

// 3 möglichkeiten toSting(), toLocalSting(), valueOf()

 //alternativ auch .join() (damit kann man alle emlemtne eines arrays in einer zeichenkette speichern, und auch gewünscht abgrenzen .join("-"))

 array=['lenny','max','john']

 array.toString()
 array.toLocaleString()