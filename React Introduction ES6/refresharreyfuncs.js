const number = [1,2,3]

const dubelnumarray = number.map((num)=>{ //Array Functions 
    return num * 2; //Hier wird über jedes Element itteriert und dann in das neue array gemapt dabei wird die methode map genutzt 
});

